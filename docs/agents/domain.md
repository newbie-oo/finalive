# Domain docs

## Layout

Single-context — one global `CONTEXT.md` and `docs/adr/` at the repo root.

## Consumer rules

- Read `CONTEXT.md` before proposing architectural changes
- Read `docs/adr/` to understand past decisions
- Write new ADRs to `docs/adr/` when a decision constrains future work
